import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.outdoormuse.app',
  appName: 'app',
  webDir: 'dist/app',
  bundledWebRuntime: false
};

export default config;
